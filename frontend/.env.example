# Next.js frontend — proxied to backend via next.config rewrites
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Express backend URL (used by Next.js rewrites at build/dev time)
BACKEND_URL=http://localhost:4000
